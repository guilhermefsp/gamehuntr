services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gamehunter
      POSTGRES_USER: gamehunter
      POSTGRES_PASSWORD: gamehunter
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
